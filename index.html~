---
layout: default
title: my blog for li.jiahan
---

<h2>{{ page.title }}</h2>

<p>Latest Blogs</p>

<ul>
		{% for post in site.posts %}
		<li>{{ post.date | date_to_string}} <a href="{{ site.baseurl }}{{ post.url}}">{{ post.title }}</a></li>
		{% endfor %}
</ul>
